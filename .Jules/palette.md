## 2024-05-18 - Semantic Tags & Deep Linking Focus States
**Learning:** Adding `aria-label` to dynamically generated tag links and semantic `<nav>` wrappers improves screen reader context significantly.
**Action:** Always use `<nav aria-label="Page tags">` for tag collections, and include `:focus-visible` with offset rings to guarantee clear keyboard focus indicators across light/dark modes.
