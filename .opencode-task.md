Fix the following issues in this Astro Starlight project:

1. **Duplicate title**: The homepage has both Starlight's default title and a custom Hero title. Remove the duplicate.

2. **MDX rendering**: The homepage content has extra `<p>` tags wrapping content that shouldn't have them. Check src/content/docs/index.mdx and fix the MDX rendering.

3. **Tags loading**: The Tag Cloud loads dynamically but may not be working properly. Check src/static/tags.js and src/components/TagCloud.astro to ensure tags load correctly.

Before making changes, run 'git fetch origin' and ensure you're working with the latest code.

Context:
- This is the Digital Garden project at /root/.openclaw/workspace-techlead/repos/blog
- It's an Astro Starlight blog deployed on Cloudflare Pages
- The AGENTS.md file in the project root contains project rules
